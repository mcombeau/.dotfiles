- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.bash:
    ~/.bashrc:
    ~/.bash_profile:
    ~/.gitmux.conf:
    ~/.shell:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:

- shell:
    - [git submodule update --init --recursive, Installing submodules]
